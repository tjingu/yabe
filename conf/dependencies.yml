# Application dependencies

require:
    - play
    - play -> crud
    - play -> docviewer

# Import the secure module
    - secure

# Import the cobertura module in application
    - play -> cobertura {version}